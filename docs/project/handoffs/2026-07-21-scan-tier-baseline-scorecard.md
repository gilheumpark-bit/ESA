# 스캔 계층 기준선 스코어카드 (2026-07-21)

## 목적·방법
- 골든: `fixtures/drawings/golden/kimm-panelboard-sld.p14.adjudicated.json` (블라인드 이중 라벨 판정본·IND-1)
- 입력: 동일 페이지의 스캔 3계층 — 클린 래스터(2382×1684) · 경열화(1.2° 회전+감광) · 중열화(2.5°+가우시안 노이즈+저대비+블러). 생성기 `scripts/fixtures/rasterize-golden-scan.mjs`
- 판독기: **프런티어 VLM 통짜(single-shot) 기준선** — CLI 구독 비전(블라인드·골든 비노출), 각 판독은 독립 1회. ※ 이것은 "빅테크 통짜" 상한 기준선이며 **ESA 파이프라인(council)의 성적이 아니다** — council 측정은 Vision 키 설정 후 `scripts/scan-golden-score.mjs` 헤더 절차.

## 결과 매트릭스 (채점: 검출·타입·정격 히스토그램 18종·환각)

| 계층 | Claude | Grok | Codex(GPT) |
|---|---|---|---|
| 클린 래스터 | **74/74·18/18·환각 0** | **74/74·18/18·환각 0** | (실행 중) |
| 경열화 | **74/74·18/18·환각 0** | 미실행 | 미실행 |
| 중열화 | **74/74·18/18·환각 0** (글리프 디테일만 정직 판독불가 격리) | 미실행 | 미실행 |

## 교차 AI 동일성 (목표: ≥90%)
- 클린 계층 Claude↔Grok: 검출·타입·정격 히스토그램 **100% 동일**. 원도면 전압 표기 상충(N19 분기 220V vs N21 헤더 440V)을 양쪽이 **독립 발견·동일하게 보류 처리** — 무발명 규율의 교차종 재현.
- Claude 계층 간(클린↔경열화↔중열화): 구조 축 100% 동일 — 열화는 글리프 수준만 침식.

## 판정·시사점
1. 스캔 클린~중열화에서 프런티어 VLM의 도달 천장 = 구조 축 100% — 계층 자체는 정복 가능 난이도.
2. 교차 AI 동일성 90% 바: **클린 계층 2사 기준 100%로 초과 실측**(n=1 도면·확대 측정은 잔여 실도면 4종으로).
3. 열화의 실침식 지점 = 미세 글리프(한글 2자·모델 끝자리·케이블 단위) — council의 4x/고대비 변형+삼중일치가 정확히 이 지점을 겨냥하므로 파이프라인이 통짜 이상을 낼 구조적 근거.
4. ESA 스캔 경로(L1→L3)의 잔여 관문: Vision 키 1개 설정 → council 3계층 채점(위 스크립트) → L3 판정선(클린 검출≥90%·정격≥85%·중열화 환각 0).

## 정직 경계
- 본 결과는 판독기(모델) 상한 기준선이지 제품 인증 아님. n=1 도면(KIMM p14). 판독 1회/칸(반복 재현성 미측정 — council 채점 시 3회 규정 적용).
